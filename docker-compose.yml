services:
  scorehub:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_CONVEX_URL=${NEXT_PUBLIC_CONVEX_URL}
    container_name: scorehub
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
